# Log Viewer 使用指南

## 快速开始

### 1. 运行程序
双击 `LogViewer.exe` 启动程序。

### 2. 导入日志格式
有两种方式创建日志格式：

#### 方式一：从文件导入（推荐）
1. 点击菜单 **File** → **Import Format**
2. 选择 `examples/apache_format.json` 或 `examples/csv_format.json`
3. 格式将自动加载到左侧面板

#### 方式二：手动创建
1. 点击菜单 **Edit** → **New Format**
2. 填写格式信息：
   - **Name**: 格式名称（如 "My Log Format"）
   - **Description**: 格式描述
   - **Delimiter**: 分隔符（空格、逗号、制表符等）
3. 添加字段：
   - 点击 **Add Field** 按钮
   - 输入字段名称和描述
4. 点击 **Save** 保存

### 3. 导入日志数据

#### 从文件导入
1. 在左侧面板选择一个日志格式
2. 点击菜单 **File** → **Import Log File**
3. 选择日志文件（如 `examples/sample_logs.txt`）
4. 日志将自动解析并显示在中间表格中

#### 导入单条日志
1. 点击菜单 **File** → **Import Single Log**
2. 在文本框中粘贴日志内容
3. 点击 **Import** 按钮

### 4. 查看日志

#### 表格视图
- 中间区域显示所有日志记录
- 点击表头可排序
- 点击某一行查看详情

#### 详情视图
- 右侧面板显示选中日志的详细信息
- 包含原始文本和解析后的字段
- 鼠标悬停在字段名上可查看字段描述

### 5. 搜索和过滤
1. 在表格上方的搜索框中输入关键词
2. 按 Enter 或失去焦点时自动搜索
3. 点击 **Clear** 清除搜索结果

### 6. 导出格式
1. 选择要导出的格式（点击左侧面板中的格式）
2. 点击菜单 **File** → **Export Format**
3. 选择保存位置并命名（.json文件）

## 日志格式定义

### JSON格式规范
```json
{
  "name": "格式名称",
  "description": "格式描述（可选）",
  "delimiter": "分隔符",
  "fields": [
    {
      "name": "字段名",
      "description": "字段描述"
    }
  ]
}
```

### 常见分隔符
- **空格**: `" "`
- **逗号**: `","`
- **制表符**: `"\t"`（需要在JSON中使用转义）
- **竖线**: `"|"`
- **分号**: `";"`
- **冒号**: `":"`

### 示例：自定义格式

#### 管道分隔日志
```json
{
  "name": "Pipe Separated Log",
  "description": "管道符分隔的日志",
  "delimiter": "|",
  "fields": [
    {"name": "timestamp", "description": "时间戳"},
    {"name": "level", "description": "日志级别"},
    {"name": "message", "description": "消息内容"}
  ]
}
```

对应日志示例：
```
2026-02-11 10:30:15|INFO|Application started
2026-02-11 10:30:20|ERROR|Database connection failed
```

## 快捷键

- **Ctrl + O**: 打开文件
- **Ctrl + S**: 保存格式
- **Ctrl + F**: 搜索
- **ESC**: 关闭对话框
- **Delete**: 删除选中项

## 高级功能

### 自动格式检测
程序支持自动检测日志格式：
1. 准备至少3条示例日志
2. 将示例传递给解析器
3. 系统会分析常见分隔符并提供建议

### 批量导入
- 支持一次性导入大型日志文件
- 使用SQLite内存数据库存储，性能优秀
- 建议单文件不超过100MB以获得最佳性能

### 数据持久化
- 格式定义保存在数据库中
- 日志数据存储在内存中（关闭程序后清空）
- 可通过导入功能重新加载数据

## 故障排除

### Q: 无法打开程序？
A: 确保您的系统支持OpenGL 3.0+，并安装了必要的运行时库。

### Q: 导入的日志解析错误？
A: 检查日志格式定义是否正确，特别是分隔符和字段数量是否匹配。

### Q: 中文显示为乱码？
A: 确保日志文件使用UTF-8编码。

### Q: 程序崩溃或卡顿？
A: 尝试减少一次导入的日志数量，或分批导入。

## 技术支持

如有问题，请联系：
- GitHub Issues: [项目地址]
- Email: support@example.com

## 许可证
MIT License - 自由使用和修改
